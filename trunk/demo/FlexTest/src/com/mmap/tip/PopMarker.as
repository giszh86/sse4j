package com.mmap.tip {	    import flash.display.Bitmap;    import flash.display.Sprite;    import flash.events.MouseEvent;    import flash.filters.BevelFilter;
    /**     * Sample Marker     * @author David Knape     */    public class PopMarker extends Sprite {        		[Embed(source='0.png')]		private var Pop:Class;        private var _title : String;                public function PopMarker(title:String=null) {        	_title = title;        	buttonMode = true;        	mouseChildren = false;        	tabEnabled = false;        	        	cacheAsBitmap = true;        	mouseEnabled = true;						addChild(new Pop() as Bitmap);        	        	filters = [ new BevelFilter(1,45,0xffffff,0.35,0x000000,0.35,2,2,1,1) ];        	        	addEventListener( MouseEvent.MOUSE_OVER, mouseOver );        }                        public function get title () : String {        	return _title;        }                public function set title (s:String) : void {        	_title = s;        }                        protected function mouseOver(e:MouseEvent) : void {       		parent.swapChildrenAt(parent.getChildIndex(this), parent.numChildren - 1);        }                override public function toString() : String {        	return '[PopMarker] ' + title;        }    }}